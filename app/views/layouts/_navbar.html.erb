<nav class="navbar navbar-inverse">
	<div class="container-fluid">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<%= link_to 'EpicQuestJourney', quests_path, class: "navbar-brand" %>
		</div>

		<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
			<% if user_signed_in? %>
				<ul class="nav navbar-nav navbar-left">
					<li class="dropdown">
						<a class="dropdown-toggle" aria-expanded="false" role="button" data-toggle="dropdown" href="#">Quests<span class="caret"></span></a>
						<ul class="dropdown-menu" role="menu">
							<li>
								<%= link_to "Active", quests_path %>
							</li>	
							<li>
								<%= link_to "Finished", finished_index_path %>
							</li>	
							<li>
								<%= link_to "Failed", unfinished_index_path %>
							</li>	
							<li class="divider"></li> 
							<li>
								<%= link_to "New Quest", new_quest_path %>
							</li>

						</ul>

					</li>
				</ul>
			<% end %>
			<ul class="nav navbar-nav navbar-right">
				<% if user_signed_in? %>
					<li>
						<%=link_to new_quest_path do%>
							<span class="glyphicon glyphicon-plus"></span>
						<%end%>
					</li>
					<li class="dropdown">
						<a class="dropdown-toggle" aria-expanded="false" role="button" data-toggle="dropdown" href="#"><span class="glyphicon glyphicon-user"></span></a>
						<ul class="dropdown-menu" role="menu">
							<li style="text-align:center">
								<div><span class="glyphicon glyphicon-user" style="font-size: 2em"></span><h4><%=link_to current_user.nickname, current_user %></h4></div>
							</li>
							<li style="text-align:center">
								<%= link_to current_user.guild.name, guild_path(current_user.guild_id) %>
							</li>
							<li class="divider"></li>
							<li>
								<%= link_to "Sign out", destroy_user_session_path, :method => :delete %></p>
							</li>
						</ul>

					</li>
				<% end %>
			</ul>
		</div>
	</div>
</nav>
